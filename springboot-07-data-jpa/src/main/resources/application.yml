spring:
  datasource:
    data-username: root
    data-password:
    url: jdbc:mysql://localhost:3306/test
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
#    更新或者创建数据库表结构
      ddl-auto: update
#      控制台显示sql
    show-sql: true
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false